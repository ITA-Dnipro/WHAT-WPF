<Window x:Class="Tetris.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tetris"
        mc:Ignorable="d"
        Title="MainWindow" Height="650" Width="500" Background="{StaticResource windowBackground}"> <!--300 300-->

    <Grid>
        <Frame Source="/Views/GameStartView.xaml"></Frame>
    </Grid>

    
    <Window.Resources>
        <ResourceDictionary Source="Styles/MainWindowStyle.xaml"/>
    </Window.Resources>

    <!--<StackPanel HorizontalAlignment="Center"
                VerticalAlignment="Center">
        <Button Style="{StaticResource startButton}">START</Button>
        <Button Style="{StaticResource startButton}">SETTINGS</Button>
        <Button Style="{StaticResource startButton}">EXIT</Button>
    </StackPanel>-->
    
</Window>
