<Window x:Class="Tetris.Views.MenuView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Tetris.Views"
      mc:Ignorable="d" 
      Width="250" Height="250"
      Title="MenuView"
      Background="{StaticResource windowBackground}"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <ResourceDictionary Source="../Styles/MainWindowStyle.xaml"/>
    </Window.Resources>

    <StackPanel HorizontalAlignment="Center"
                VerticalAlignment="Center">
        <Button Style="{StaticResource startButton}"
                Click="Button_Click">START</Button>
        <Button Style="{StaticResource startButton}" Click="Button_Exit">EXIT</Button>
    </StackPanel>
</Window>
